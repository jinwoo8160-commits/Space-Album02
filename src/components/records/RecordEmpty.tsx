import { EmptyTabPage } from "@/components/layout/EmptyTabPage";

/** 하단 기록 탭. 아직 본문이 없어 안내만 보여 줍니다. */
export function RecordEmpty() {
  return (
    <EmptyTabPage title="기록" description="기록 페이지 준비 중입니다" />
  );
}
